# 交接文档（dsh-feishu-bot）

> **维护者必读**。新接入者请先看 [初始化文档.md](初始化文档.md)。
> 本文档是项目的**唯一权威交接记录**：任何功能变更完成后，**必须**在本文档
> 「变更记录」章节追加一条记录（见文末规定）。

## 1. 项目概览

| 项 | 值 |
|---|---|
| 仓库 | <https://github.com/wangsan71/DSh-feishu-bot>（分支 `main`，个人仓库） |
| npm 包 | `@areoneplayer/dsh-feishu-bot`（`publishConfig.access: public`） |
| 当前版本 | 0.1.1 |
| 许可证 | BSD-3-Clause |
| 作用 | 让 DSH 通过飞书/Lark 聊天：机器人长连接收事件 → 每聊天绑定独立 DSH agent 自动回复；支持国内外双域；对话命令新建/切换工作区与会话；agent 可向飞书用户提问、发通知、发文件；交互卡片选择 |
| 插件形态 | DSH 插件（Host 半区 + 浏览器半区），经 `cordis.patch.yml` 注册 |

## 2. 架构与数据流

### 2.1 总体结构

```
飞书/Lark 服务器
   │  WebSocket 长连接（无需公网 URL）
   ▼
bridge/ws-bridge.cjs  ←── 独立子进程（官方 SDK，watchdog 自动重启）
   │  POST http://127.0.0.1:<port>/feishu/event   {type: event|card}
   ▼
src/index.ts（Host 半区，运行在 DSH 进程内）
   ├─ /feishu/event    事件入口（url_verification / card / message）
   ├─ /feishu/health   健康检查
   ├─ /feishu/api/*    status / config（供浏览器设置页调用）
   ├─ handleMessage    文本消息 → 绑定 agent 回复 / 处理命令 / 解析提问答案
   ├─ handleCard       卡片按钮点击（带 context.open_chat_id）
   ├─ bindAgentSession 创建/恢复 agent（注入默认 provider/model）
   ├─ userQuestions    DSH 提问转发到飞书（pendingQuestions 映射，300s 超时）
   └─ 工具 lark_status / lark_configure / lark_test / lark_notify（任意 DSH 对话
      向飞书发通知，格式 [工作区]-[对话]：[内容]，目标取配置 notify_chat_id）/
      lark_send_file（上传本机文件 ≤30MB 并发文件消息；uploadFile 手动构造
      multipart，file_key 读取响应 data.file_key 嵌套层）
   │
   ▼
src/client/index.ts（浏览器半区）→ settings.plugins.tab 设置页
```

### 2.2 关键设计决策（勿随意更改）

1. **长连接优先**：事件经官方 SDK WSClient 接收，转发给本机 DSH 路由。无需公网
   IP / 域名 / 回调配置。Webhook 模式仅作兼容保留。
2. **Domain 是数字枚举**：SDK `Lark.Domain.Lark`(1) / `Lark.Domain.Feishu`(0)，
   传字符串会导致 ws 端点 URL 非法（`Invalid URL`）。桥接与 host 均已转换。
3. **agent 必须注入模型**：`agents.create/resume` 必须带
   `agentOptions.provider/model`（取自 `agentDefaultModel.currentSelection()`），
   否则回合被 pre-step 拒绝，表现为「收到消息但不回复」。
4. **状态文件分离**：
   - 配置 `~/.dsh/dsh-feishu-bot.json`（app_id / app_secret / domain / cwd …）
   - 状态 `~/.dsh/dsh-feishu-bot-state.json`（每聊天 `{workspaceId, sessionKey}`）
   - 桥接日志 `~/.dsh/dsh-feishu-bot/bridge.log`
5. **机器人自定义菜单已移除**（`application.bot.menu_v6` 不再注册/转发，保留对话
   命令 + 卡片选择）。如需恢复，需在 bridge 注册事件、host 加回 handleMenu 与
   eventHandler 分支、并同步 README 菜单表。
6. **BOM 防御**：配置文件可能被 PowerShell 写入 UTF-8 BOM，加载时统一剥离
   `\uFEFF`，并支持首次消息时惰性重载自愈。
7. **bridge 是 .cjs**：包为 `"type":"module"`，桥接用 CommonJS 必须 `.cjs` 后缀，
   且**不进入 tsdown 构建**（纯 JS 资产，运行时 `node bridge/ws-bridge.cjs` 拉起）。
8. **文件发送**：`lark_send_file` = 上传（手动 multipart，`POST /im/v1/files`，
   响应 `data.file_key` 在嵌套层）+ 发送（`msg_type: file`）。需 `im:resource`
   权限；≤30MB、非空。

## 3. 开发规范（官方格式）— 增加功能 / 修改代码必读

> **硬性规定**：对本插件的任何新增功能或代码修改，必须遵循以下官方格式与流程，
> 违者提交会被拒绝。

### 3.1 目录与文件职责（不可随意新增根级散文件）

| 路径 | 职责 | 变更规则 |
|---|---|---|
| `src/index.ts` | Host 半区全部逻辑 | 新功能优先放这里；超过 800 行可拆 `src/` 子模块（按官方模块风格） |
| `src/client/index.ts` | 浏览器半区设置页 | 仅当需要新 UI 时改 |
| `bridge/ws-bridge.cjs` | 长连接桥接 | 新事件类型在此注册并转发；改动后 kill 旧桥接即自动重启 |
| `scripts/send-notify.cjs` | 通知发送脚本（Node UTF-8） | 纯 ASCII；改发送逻辑时保持 UTF-8 字节发送 |
| `cordis.patch.yml` | 插件注册行 | 一般不动；改插件名/ID 时才动 |
| `build/` | vendored 构建预设 | 不修改（来自 dsh-web-ui 全家桶，独立维护） |
| `tests/load.spec.ts` | 冒烟测试 | 新增导出时同步补断言 |
| `初始化文档.md` / `交接文档.md` | 文档 | 行为/流程变化必须同步更新 |

### 3.2 依赖声明三处一致

新增依赖必须同时出现在（缺一不可）：
1. `package.json` 的 `peerDependencies`（运行时由宿主注入）与 `devDependencies`（构建期）；
2. `tsdown.config.ts` 的 `libExternal` 列表（避免把宿主服务打进 bundle）；
3. Host 半区 `inject` 数组（`src/index.ts` 顶部）——未声明就访问 `ctx.xxx` 属违规。

### 3.3 代码风格（对齐官方 DSH 插件规范）

- Host/Client 半区导出固定三件套：`export const name`、`export const inject`、
  `export function apply(ctx, config?)`。
- 所有服务必须经 `inject` 声明后才可访问；可选用 `ctx.get()` 判空。
- 副作用（定时器、路由、事件监听、工具注册）必须可逆：用 `ctx.effect()` /
  `ctx.on()` 注册，或返回 disposer，保证插件 stop/update 时全部清理。
- 新的事件类型：桥接 `EventDispatcher.register` + host `handleXxx(parsed)` 成对添加；
  转发 payload 统一 `{type, header, event}` 结构。
- 字符串消息处理走 `handleMessage` 的现有命令解析（勿另起解析器）。
- 注释/用户可见文案：中英双语或至少中文；新增菜单 key 同步更新 README 菜单表。

### 3.4 提交前检查（CI 会强制）

```sh
npm run typecheck   # tsc --noEmit
npm run build       # tsc + tsdown
npm test            # vitest
```

本地全部通过后再 commit；CI（`.github/workflows/ci.yml`）在 push/PR 时重复以上三步。

### 3.5 提交信息格式（Conventional Commits）

`<type>: <subject>`，type ∈ `feat | fix | docs | chore | ci | refactor | test`。
示例：`feat: relay agent questions to Feishu chat`。

### 3.6 版本与发布流程

1. 改完代码 → 按上面检查 → 合并到 `main`。
2. `npm version patch/minor`（同步更新 package.json 与 package-lock）。
3. 打 tag：`git tag v<新版本>` → `git push origin main --tags`。
4. GitHub Actions `release.yml` 自动 `npm publish`（需要仓库 `NPM_TOKEN` secret，
   且 token 必须带 bypass 2FA 权限；当前账号 2FA 已禁用）。
5. **完成后必须在本文档「变更记录」追加一条**（见 3.7），并推送。

> 发布前确认 `package.json` 的 `files` 已包含新产物（lib、bridge、scripts、
> cordis.patch.yml、README*）。

### 3.7 变更记录硬性规定

- 每次合并功能/修复后，必须在「第 6 章 变更记录」**顶部插入**一条：
  `- YYYY-MM-DD（版本 vX.Y.Z）：一句话描述变更 + 关联 commit（如有）`。
- 文档提及的行为（菜单 key、配置文件字段、工具名、路由）若随变更改变，
  必须同步更新本文档与 README，保持**三处一致**（代码 / README / 交接文档）。

## 4. 发布渠道与安装方式

| 渠道 | 命令 | 适用 |
|---|---|---|
| git 安装 | `dsh plugin --profile web add git+<repo-url>` | 普通用户，无需 npm 包 |
| link 安装 | `dsh plugin --profile web add link:<本地路径>` | 本地开发 |
| npm 安装 | `dsh plugin --profile web add @areoneplayer/dsh-feishu-bot` | 已发布版本 |

当前本机 profile：`~/.dsh/profiles/web/node_modules/@areoneplayer/dsh-feishu-bot`
（junction 指向本地仓库，重装后已切换为 @areoneplayer 名）。

## 5. 安全与运维注意事项

- `app_secret` 明文存 `~/.dsh/dsh-feishu-bot.json`（权限 0600），**不得写入日志/文档**。
- npm token 属敏感信息：仓库 `.npmrc` 已 gitignore；对话中暴露过的 token 应立即吊销。
- 每个飞书聊天绑定独立 DSH agent，会消耗 API 额度；agent 带全部工具，可执行文件操作。
- 长连接断开时看门狗 5 秒自动重启桥接；重启期间消息可能丢失，属预期。
- 断线重连可能重放非幂等命令（agent 侧），改动命令处理时注意幂等性。
- `open_id` 是**按应用隔离**的：测试菜单事件时，必须用本应用成员接口
  （`/im/v1/chats/{chat_id}/members?member_id_type=open_id`）取真实 open_id，
  跨应用 open_id 会报 `open_id cross app`（99992361）。

## 5.5 通知脚本（编码一劳永逸方案）

向飞书发通知**只走两条路**，二者都是 UTF-8 安全的：

1. **`lark_notify` 工具**（推荐，agent 用）：任何 DSH 对话的 agent 调用，
   插件 Node 侧发送，UTF-8 全程正确。
2. **`scripts/send-notify.cjs`**（shell/手动用）：
   `node scripts/send-notify.cjs --text "你好" [--workspace X] [--session Y] [--chat-id oc_xxx]`
   - Node 在 Windows 接收 argv 为 UTF-16LE，中文参数不受控制台代码页影响；
     文件读写与 HTTP 也全 UTF-8（与插件桥接同栈）。
   - 目标选择：`--chat-id` > 配置 `notify_chat_id` > 状态文件第一个聊天。
   - 纯 ASCII 脚本（无中文字面量），任意代码页都能解析。

**严禁**用 PowerShell 5.1 的 `Invoke-RestMethod` 手写发送中文——其默认把字符串
body 按 ANSI/GBK 编码，中文会变成 `?????`（本仓库曾踩过此坑，勿复现）。

## 6. 变更记录

- 2025-08-19（版本 **0.1.6**）：**发布 npm 0.1.6**——完善锁定会话的「飞书对话模式」
  提示词：①回复简洁（短句/要点）；②DSh 内执行、飞书只收结论；③推送用
  lark_notify、发文件用 lark_send_file；④长内容落文件发送。tag `v0.1.6`
  由 Actions 自动发布（commit `972873c` / `803b077`）。
- 2025-08-19（版本 0.1.5→0.1.6 待发）：完善锁定会话的「飞书对话模式」提示词——
  ①回复必须简洁（短句/要点，避免长报告）；②需 DSh 能力执行的工作（读写文件、
  命令、搜索、代码等）照常在 DSh 内完成，结论+关键结果简洁回复到飞书；
  ③推送结果用 lark_notify、发文件用 lark_send_file；④长内容优先落成文件发送
  而非贴大段文字（commit `972873c`）。
- 2025-08-19（版本 **0.1.5**）：**发布 npm 0.1.5**——新增会话头部「锁飞书默认」
  三点菜单（`conversation.session.header.utilities` add-on 位 + `/feishu/api/lock-*`
  路由，锁定/解除 notify_chat_id，持久化到配置文件）；锁定会话的 agent 注入
  「在飞书回复」scoped systemPrompt section（`applyLockedContext`，bind 时 +
  锁定按钮即时触发）。tag `v0.1.5` 由 Actions 自动发布。
- 2025-08-19（版本 **0.1.4**）：**发布 npm 0.1.4**——移除机器人自定义菜单
  （`menu_v6` 不再注册/转发，保留对话命令与卡片选择）；新增 `lark_send_file`
  工具（上传本机文件 ≤30MB 并发文件消息，`im:resource` 权限）；修复 file_key
  读取层级（响应 `data.file_key` 嵌套）。tag `v0.1.4` 由 GitHub Actions 自动
  发布成功（NPM_TOKEN 配置生效后首次自动发布）。
- 2025-08-19（版本 0.1.3→0.1.4 待发）：**移除机器人自定义菜单**（`menu_v6` 不再
  注册/转发，保留对话命令与卡片选择）；**新增 `lark_send_file` 工具**——上传
  本机文件（≤30MB）并发文件消息到飞书（上传手动 multipart，实测通过：
  `im:resource` 权限已开通）；修复 file_key 读取层级（响应 `data.file_key`
  嵌套）；README/初始化/交接/AGENTS 同步（feat: drop menu + send_file,
  commit `7d8ef30` / `33d134a`）。
- 2025-08-19（版本 **0.1.2**）：**发布 npm 0.1.2**——含 render 双参修复、工具 schema
  声明、lark_notify、UTF-8 通知脚本、桥接静默、P0 webProfile 修复等全部改动
  （commit `46c8711` 起至 `2de6c79`，tag `v0.1.2`）；GitHub Actions release 因
  `NPM_TOKEN` secret 未配置失败，已手动发布（token 用后即清，勿留 `.npmrc`）。
- 2025-（版本 0.1.1）：**修复工具 render 签名 bug**——DSH 工具契约要求
  `render(args, value)` 双参；此前单参实现把 `args`（空对象）当 value 序列化，
  导致所有工具（lark_status/configure/test/notify）向模型展示的都是 `{}`，
  真实返回值从未被看到（自首个版本 `b2fdadc` 起即存在）。改为双参后
  （fix: render(args, value) two-arg signature, commit `46c8711`）。
- 2025-（版本 0.1.1）：P0 事故修复——`dsh-user-questions` 是严格单例，Web profile
  下 `dsh-host-apiproxy` 已占住 provider，`dsh-feishu-bot` 无条件注册会导致
  `DUPLICATE_PROVIDER`、dsh web 无法启动；修复为检测 Web profile 跳过注册
  （提问转发仅在 headless/CLI profile 生效），并从 profile bundles 移除功能
  重叠的 `@xmanrui/dsh-im`。详见 [docs/incident-2026-08-18-dsh-feishu-conflict-fix.md](docs/incident-2026-08-18-dsh-feishu-conflict-fix.md)。
- 2025-（版本 0.1.1）：修复工具输出 schema（声明完整 properties，此前返回值
  被过滤成空对象 `{}`）；桥接 stdout/stderr 改为受限收集（不再刷屏 dsh web
  控制台；SDK 中文乱码只是控制台 GBK 显示问题，bridge.log 文件本身 UTF-8 正确）
  （fix: tool output schemas + quiet bridge stdio）。
- 2025-（版本 0.1.1）：修复中文通知乱码（`?????`）——根因是 PowerShell 5.1
  `Invoke-RestMethod` 按 ANSI 编码字符串 body；新增 `scripts/send-notify.cjs`
  （Node 全程 UTF-8，argv 为 UTF-16LE），通知统一走 `lark_notify` 工具或该脚本；
  初始化/交接/README 文档同步（fix: UTF-8 notification script）。
- 2025-（版本 0.1.1）：新增 `lark_notify` 工具——任意 DSH 对话可向飞书发通知，
  格式 `[工作区]-[对话]：[内容]`，目标默认取配置 `notify_chat_id`（也可自动回
  调用者绑定的飞书聊天）；`lark_configure` / `/feishu/api/config` 支持该字段；
  GUIDANCE / README / 初始化文档 / 交接文档同步（feat: lark_notify interface）。
- 2025-（版本 0.1.1）：新增 [初始化文档.md](初始化文档.md)（新接入/新开发者从零初始化）
  与本文档（维护者交接）；AGENTS.md 固化「按官方格式开发 + 完成后更新交接文档」规定；
  README 双语补充卡片选择/提问转发说明与文档链接（docs: init & handover docs）。
- 2025-08-（版本 0.1.1→）：新增提问转发（ask_user_question 到飞书）+ 交互卡片菜单选择
  （change_session/change_workspace 弹卡片按钮），桥接新增 `card.action.trigger` 转发
  （commit `efb9734`）；README 双语同步（`4377bc0`）。
- 2025-08-（版本 0.1.1）：机器人自定义菜单事件 `application.bot.menu_v6` 支持
  （commit `b007faa`、`887d4da`）；包名改为 `@areoneplayer/dsh-feishu-bot` 并发布 npm
  （`86510a6`、`d67c12a`、`02dc7e4`）。
- 2025-08-（版本 0.1.0）：首个可用版本——长连接桥接、双域探测、每聊天 agent 绑定、
  菜单命令 NEW_WORKSPACE / NEW_SESSION / WORKSPACE_CHANGE / CHANGE_SESSION、工具
  lark_status / lark_configure / lark_test、Web 设置页（commit `b2fdadc` 起）。
