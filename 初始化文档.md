# 初始化文档（dsh-feishu-bot）

> 面向**新接入者 / 新开发者**：从零把本插件跑起来，并接入自己的飞书/Lark 应用。
> 维护者约定见 [交接文档.md](交接文档.md)。

## 0. 前置条件

| 项 | 要求 |
|---|---|
| Node.js | ≥ 22（含 npm） |
| DSH | 已安装 `dsh` CLI 与 web 桌面端（`dsh web`） |
| 飞书/Lark | 一个开发者账号（扫码即可），用于创建机器人应用 |

> 国内（feishu，open.feishu.cn）与国际（lark，open.larksuite.com）均可；
> 插件按 `domain` 配置自动选择端点，`auto` 会先探测国内再探测国际。

## 1. 获取代码（二选一）

```sh
# A. 作为 DSH 插件直接安装（推荐给普通用户）：
#    git 方式（无需 npm 包）
dsh plugin --profile web add git+https://github.com/wangsan71/DSh-feishu-bot.git

# B. 本地开发（推荐给开发者）：
git clone https://github.com/wangsan71/DSh-feishu-bot.git
cd DSh-feishu-bot
dsh plugin --profile web add link:<本仓库绝对路径>
```

npm 安装方式（已发布包 `@areoneplayer/dsh-feishu-bot`）：

```sh
dsh plugin --profile web add @areoneplayer/dsh-feishu-bot
```

安装后**重启 `dsh web`**，让新插件（含浏览器半区）加载。

## 2. 本地开发依赖与构建（仅开发者）

```sh
npm install          # 或 pnpm install
npm run typecheck    # tsc 类型检查（提交前必须通过）
npm run build        # tsc + tsdown（产出 lib/ 与 lib/client.js）
npm test             # vitest 冒烟测试（提交前必须通过）
```

构建产物 `lib/` 是运行时实际加载的代码；**改完源码必须重新 build**，
DSH 进程不会热替换 lib，需重启 `dsh web` 生效。

## 3. 创建飞书/Lark 机器人应用（约两分钟）

1. 打开开发者后台并扫码登录：
   - 国内：<https://open.feishu.cn/app>
   - 国际：<https://open.larksuite.com/app>
2. 「创建企业自建应用」→ 填名称/图标 → 创建。
3. 记下 **App ID**（`cli_...`）与 **App Secret**（「凭证与基础信息」页）。
4. 开启机器人能力：应用能力 → 机器人 → 启用。
5. （可选，推荐）配置自定义菜单：应用能力 → 机器人 → 菜单配置，
   事件类型选择 `application.bot.menu_v6`，菜单项 key 用：
   `new_workspace`、`new_session`、`change_workspace`、`change_session`、`help`
   （可配中文文案，见 README 菜单表）。
6. 发布版本（版本管理与发布 → 创建版本 → 申请发布），否则只有你自己能收事件。

> 本插件走**长连接（WebSocket）**接收事件，**不需要**在后台配置「事件订阅 URL」、
> 不需要公网 IP、不需要域名回调。长连接是自动建立的，无需手工填任何回调地址。

## 4. 配置插件（三选一）

### A. Web 设置页（推荐给新手）

`dsh web` 侧边栏「设置 → 插件 → Feishu/Lark Bot」，填 AppID/Secret，点保存。
页面顶部有开发者后台二维码，扫码直达创建应用页。

### B. 让 DSH agent 帮你配置

把「帮我配置飞书机器人」告诉任意 DSH agent，它会调用 `lark_configure` 工具完成。

### C. 直接改配置文件

编辑 `~/.dsh/dsh-feishu-bot.json`：

```json
{
  "app_id": "cli_xxxxxxxxxxxxxxxx",
  "app_secret": "xxxxxxxxxxxxxxxxxxxxxxxx",
  "domain": "auto",
  "websocket": true,
  "cwd": "D:/你的工作目录"
}
```

- `domain`：`feishu`（国内）/ `lark`（国际）/ `auto`（默认，自动探测）
- `websocket`：`true` 走长连接（默认）；`false` 走 Webhook 模式（需在后台配
  事件订阅 URL 指向本机 `/feishu/event`，通常不建议）
- `cwd`：机器人每会话 agent 的工作目录（默认 DSH 会话目录）
- `agent_preset`：可选，指定该 agent 使用的预设

配置由插件热加载并自动重启桥接进程，**无需重启 dsh web**。

## 5. 验证

1. 在 DSH 里问 agent：「飞书机器人状态如何？」→ 调用 `lark_status`，
   应显示已配置、域、桥接运行中。
2. 用测试工具：agent 调用 `lark_test`，应返回 token 获取成功、桥接在线。
3. 在飞书里**单聊你的机器人**，发一条消息 → 机器人应回复（首次会自动创建
   绑定该聊天的 DSH agent 会话并执行回复）。
4. 测试菜单：点击菜单「切换工作区 / 切换会话」→ 应弹出交互卡片，
   点按钮即可切换；也可直接回复编号。
5. 测试提问转发：让该聊天的 agent 调用「提问」工具（ask_user_question），
   问题应出现在飞书聊天里，你的回复会作为答案返回给 agent。

## 6. 常见问题（FAQ）

| 现象 | 原因与处理 |
|---|---|
| 机器人不回复，事件也收不到 | 应用未发布版本；或长连接桥接未运行（看 `~/.dsh/dsh-feishu-bot/bridge.log`）；或 AppSecret 填错 |
| 配置后仍显示未配置 | 配置文件带 UTF-8 BOM（PowerShell 写入导致）。用 Web 设置页或 `lark_configure` 重写一次即可，插件会自动剥离 BOM |
| `Invalid URL` / ws 端点异常 | `domain` 传了字符串给 SDK，必须用数字枚举（Feishu=0 / Lark=1）。桥接已内置转换，正常情况下不会出现 |
| 菜单点击无反应 | 后台未开启 `application.bot.menu_v6` 事件；菜单事件无 chat_id，回复发到点击者的 open_id（单聊） |
| agent 创建后不回消息（回合 blocked） | 创建 agent 时必须注入默认 provider/model（`agentDefaultModel.currentSelection()`），插件已内置 |
| 卡片按钮点击无反应 | 需重启 dsh web 加载新桥接；检查桥接日志是否有 `card.action.trigger` 转发 |

桥接日志：`~/.dsh/dsh-feishu-bot/bridge.log`（桥接由插件看门狗自动重启，改完
`bridge/ws-bridge.cjs` 后 kill 掉旧进程即可自动拉起新版本）。

## 7. 目录速览

```
dsh-feishu-bot/
├─ src/index.ts            # Host 半区：引擎、路由、agent 绑定、工具、提问转发
├─ src/client/index.ts     # 浏览器半区：设置页（settings.plugins.tab）
├─ bridge/ws-bridge.cjs    # 长连接桥接子进程（官方 SDK，纯 JS，不走 tsdown）
├─ cordis.patch.yml        # DSH 插件注册行（bundle patch）
├─ build/                  # vendored 构建预设（tsdown，来自 dsh-web-ui 全家桶）
├─ tests/load.spec.ts      # vitest 冒烟测试
└─ 交接文档.md              # 维护者必读：架构、官方格式、发布流程、变更记录
```

> **新增功能 / 修改代码前，先读 [交接文档.md](交接文档.md) 的「开发规范（官方格式）」章节。**
